package hwr.oop.skat.gruppe2;

import hwr.oop.skat.gruppe2.application.SpielVerwaltung;
import hwr.oop.skat.gruppe2.domain.Spieler;
import hwr.oop.skat.gruppe2.domain.Spielfeld;
import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.Test;

import java.util.ArrayList;
import java.util.List;
import java.util.UUID;

public class SpielVerwaltungTest {
  /*@Test
  public void testNeuesSpiel() {
    List<String> testSpielerStrings = List.of(UUID.randomUUID().toString(), UUID.randomUUID().toString(), UUID.randomUUID().toString());
    //reaches here
    SpielVerwaltung testSpielVerwaltung = new SpielVerwaltung(testSpielerStrings);
    //Assertions.assertThat(testSpielVerwaltung.getUUID()).isNotEqualTo(null);
  }*/
}
